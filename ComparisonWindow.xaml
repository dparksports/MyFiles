<Window x:Class="FileLister.ComparisonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Comparison Tool" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="File Comparison" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                <TextBlock Text="Old File (Base):"/>
                <TextBox x:Name="FileAText" IsReadOnly="True" Margin="0,5,0,5"/>
                <Button Content="Browse..." Click="BrowseA_Click"/>
            </StackPanel>
            
            <TextBlock Grid.Column="1" Text="VS" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0"/>

            <StackPanel Grid.Column="2" Margin="5,0,0,0">
                <TextBlock Text="New File (Target):"/>
                <TextBox x:Name="FileBText" IsReadOnly="True" Margin="0,5,0,5"/>
                <Button Content="Browse..." Click="BrowseB_Click"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Mode:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="ModeCombo" SelectedIndex="0" Width="200" Margin="0,0,10,0">
                <ComboBoxItem Content="Checksum Comparison (Detailed)"/>
                <ComboBoxItem Content="Simple File List Comparison"/>
            </ComboBox>
            <Button Content="Compare" Padding="20,5" Click="Compare_Click"/>
        </StackPanel>

        <DataGrid x:Name="ResultsGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="File Path" Binding="{Binding FilePath}" Width="*"/>
                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="4" Content="Export Report to CSV" HorizontalAlignment="Right" Padding="10,5" Click="Export_Click"/>
    </Grid>
</Window>
